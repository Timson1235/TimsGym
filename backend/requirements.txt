# TimsGym Python backend dependencies
fastapi>=0.115
uvicorn[standard]>=0.30

# Phase 1: database + auth
sqlmodel>=0.0.22          # ORM (SQLAlchemy + Pydantic), talks to the same Neon DB
psycopg2-binary>=2.9      # Postgres driver
google-auth>=2.30         # verify Firebase login tokens (public-cert check, no service-account file)
pydantic-settings>=2.4    # typed config from .env

# Phase 2: AI agent
google-genai>=1.0         # Gemini SDK (chat + function-calling)
